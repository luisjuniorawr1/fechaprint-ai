{
  "name": "fechaprint-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "firebase:login": "npx firebase-tools login",
    "firebase:serve": "npx firebase-tools emulators:start --only hosting",
    "firebase:deploy": "npx firebase-tools deploy --only hosting --project fechaprint"
  }
}
